DISCORD_TOKEN=YOUR_TOKEN_HERE
PREFIX=!
USE_MEMBERS_INTENT=0
############################################################
# Exemple de configuration pour Nyah-Chan (.env)
# Copiez ce fichier en ".env" et remplissez les valeurs.
############################################################

# --- Discord ---
DISCORD_TOKEN=YOUR_TOKEN
PREFIX=!
USE_MEMBERS_INTENT=0
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# --- Réactions ---
# 1 pour activer, 0 pour désactiver l'ajout d'emoji sur les messages
REACTIONS_ENABLED=0

# --- Triggers de rôles ---
# Fichier JSON optionnel pour définir plusieurs paires trigger->rôle
# (ex: role_triggers.json). Laissez vide pour utiliser les variables fallback ci-dessous.
ROLE_TRIGGERS_CONFIG=
# Fallback si pas de fichier JSON : un seul trigger
ROLE_NAME=Nom Du Rôle à créer
TRIGGER_WORD=Mot qui attribuera le rôle
REMOVE_TRIGGER=Mot ou chaine qui retirera le rôle

# --- Grant commands (donner un rôle via commande) ---
# Fichier JSON optionnel (ex: grant_commands.json). Voir grant_commands.example.json
GRANT_COMMANDS_CONFIG=
# Fallback si pas de fichier JSON : une seule commande
GRANT_CMD_NAME=commande à jouer
GRANT_ROLE_NAME=Rôle attribué par la commande
# IDs séparés par des virgules
GRANT_ALLOWED_USER_IDS=123456789012345678,987654321098765432
# Optionnel: chemin vers un GIF envoyé quand la commande réussit
GRANT_GIF_PATH=pokeball-fable.gif

# --- Keyword responses (embeds déclenchés par mots-clés) ---
# Fichier JSON optionnel pour définir des embeds déclenchés par des mots-clés.
# Voir keyword_responses.example.json pour un exemple complet de structure.
# Laisser vide pour utiliser le chemin par défaut "keyword_responses.json" à la racine.
KEYWORD_RESPONSES_CONFIG=

# --- Ollama (Q&A en mention du bot) ---
# Mettre à 1 pour activer la réponse via Ollama quand le bot est mentionné
OLLAMA_ENABLED=0
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
OLLAMA_TIMEOUT=60
